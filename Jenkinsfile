podTemplate(containers: [
  ]) {

    node(POD_LABEL) {
        stage('test') {
            container('jnlp') {
                stage('test') {
                    sh 'echo "hi'
                }
            }
        }

    }
}